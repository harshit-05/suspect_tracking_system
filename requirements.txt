# Interim dependency manifest (STORY-003; closes audit finding C5).
# Full pyproject.toml + uv lockfile migration is Phase 2 (SRS NFR-REP-1).
#
# Python: 3.12 (tested on CPython 3.12.13; 3.11 also acceptable — SRS floor is 3.11.
#         3.14 system python is NOT supported by these pins).
# Install with uv (operator directive — no bare pip, no conda):
#
#   uv venv --python 3.12 .venv
#   uv pip install -r requirements.txt                                   # GPU box (CUDA wheels)
#   uv pip install -r requirements.txt \
#       --extra-index-url https://download.pytorch.org/whl/cpu \
#       --index-strategy unsafe-best-match                              # CPU-only dev box
#
# torch/torchvision are pinned without the "+cpu" local tag so the same file
# resolves CUDA wheels on the GPU deployment machine (device stays modular —
# never hardcode cpu/cuda in code).

torch==2.13.0
torchvision==0.28.0
ultralytics==8.4.102
opencv-python==5.0.0.93
numpy==2.4.4
deep-sort-realtime==1.3.2
deap==1.4.4
matplotlib==3.11.1
tqdm==4.69.0
pyyaml==6.0.3
natsort==8.4.0
# PyPI motmetrics==1.4.0 uses np.asfarray (removed in NumPy 2) — pin the fixed
# upstream commit until a release lands (STORY-006):
motmetrics @ git+https://github.com/cheind/py-motmetrics@e5ae766fab03b804f485d7c95ee1809b94d32dcc

# --- deliberately excluded ---
# yolox: imported only by trackers/bytetrack_wrapper.py, which audit finding C4
# shows calls BYTETracker with a wrong constructor and update() signature — it
# never worked against any stock YOLOX release, so there is no "known-good"
# version to record. YOLOX is also awkward to pip-install (no maintained wheel
# for modern Python). Consequently bytetrack_wrapper.py and main.py (which
# imports it) do NOT import in this environment — accepted and documented.
# Phase 1 (SRS FR-TRK-2) replaces the wrapper with the `supervision` library's
# maintained ByteTrack; do not install yolox in the meantime.
